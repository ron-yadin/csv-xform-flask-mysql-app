# Rename this file from env.txt to .env before use
# For security, replace default values for the 4 required environment variables with custom values
# Note: if special characters are desired, use double quotes (") to wrap the value - ex: MYSQL_PASSWORD="$pe(i@l_pa##word"
MYSQL_ROOT_PASSWORD=default_root_password
MYSQL_DATABASE=default_database_name
MYSQL_USER=default_user
MYSQL_PASSWORD=default_password